namespace Problem_Solving.NeetCode.Day_17
{
    internal class CheckInclusion
    {
        //Console.WriteLine(program.CheckInclusion("abc", "lecaabee"));

        //public bool CheckInclusion(string s1, string s2)
        //{
        //    char[] s1Sorted, substring;
        //    s1Sorted = s1.ToCharArray();
        //    Array.Sort(s1Sorted);

        //    for (int i = 0; i < s2.Length - s1.Length + 1; i++)
        //    {
        //        substring = s2.Substring(i, s1.Length).ToCharArray();
        //        Array.Sort(substring);
        //        if (substring.SequenceEqual(s1Sorted))
        //            return true;
        //    }
        //    return false;
        //}
    }
}
