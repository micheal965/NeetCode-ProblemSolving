namespace Problem_Solving.NeetCode.Day_11
{
    internal class ThreeSum
    {
        //Program program = new Program();
        //program.ThreeSum([-1, 0, 1, 2, -1, -4]);

        //public List<List<int>> ThreeSum(int[] nums)
        //{
        //    HashSet<Tuple<int, int, int>> uniqueTriplets = new HashSet<Tuple<int, int, int>>();
        //    List<List<int>> res = new List<List<int>>();
        //    Array.Sort(nums);
        //    int a, b, c;

        //    for (int i = 0; i < nums.Length; i++)
        //    {
        //        a = nums[i];
        //        for (int j = i + 1; j < nums.Length; j++)
        //        {
        //            b = nums[j];
        //            for (int k = j + 1; k < nums.Length; k++)
        //            {
        //                c = nums[k];
        //                if (a + b + c == 0)
        //                {
        //                    var triplet = Tuple.Create(a, b, c);
        //                    uniqueTriplets.Add(triplet);
        //                }
        //            }
        //        }
        //    }
        //    foreach (var triplet in uniqueTriplets)
        //        res.Add(new List<int> { triplet.Item1, triplet.Item2, triplet.Item3 });

        //    return res;
        //}
    }
}
