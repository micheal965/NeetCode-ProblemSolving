namespace Problem_Solving.NeetCode.Day_7
{
    internal class IsPalindrome
    {

        //IsPalindrome("Was it a car or a cat I saw?");

        //public static bool IsPalindrome(string s)
        //{
        //    var CorrectedStr = "";
        //    foreach (var item in s)
        //    {
        //        if (char.IsLetterOrDigit(item))
        //            CorrectedStr += item;
        //    }
        //    for (int i = 0, j = CorrectedStr.Length - 1; i < CorrectedStr.Length / 2; i++, j--)
        //        if (char.ToLower(CorrectedStr[i]) != char.ToLower(CorrectedStr[j]))
        //            return false;
        //    return true;
        //}
        //public static bool IsPalindrome(string s)
        //{
        //    var CorrectedStr = "";
        //    foreach (var item in s)
        //        if (char.IsLetterOrDigit(item))
        //            CorrectedStr += char.ToLower(item);
        //    return CorrectedStr == new string(CorrectedStr.Reverse().ToArray());
        //}
    }
}
